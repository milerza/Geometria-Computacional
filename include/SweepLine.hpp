#ifndef SWEEP_LINE
#define SWEEP_LINE

#include <vector>
#include <utility>
#include "Point.hpp"

// Primitivas
int direction(point p1, point p2, point p3);
bool intersect(point p1, point p2, point p3, point p4);

// SweepLine

#endif